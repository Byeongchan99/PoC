# 기차 게임 PoC - Claude 작업 지침

## 프로젝트 정보
- Unity 6 기반 Unity 2D 프로젝트

## 스크립트 작성 규칙

### 사전 확인
- 스크립트를 새로 작성하기 전, 어떤 스크립트를 작성할지 반드시 사용자에게 먼저 확인받을 것

### Inspector 노출
- 디버그 및 수치 조정을 위해 Inspector에서 수정 가능하도록 `[SerializeField]`를 적극 활용할 것

### 주석 처리된 코드
- 사용자가 주석 처리한 코드는 수정 시 건드리지 말 것

## 코드 품질

### 명명 규칙
- Unity 공식 가이드라인 준수
  - 클래스, 메서드, 프로퍼티: PascalCase
  - 지역 변수, 파라미터: camelCase
  - private 필드: _camelCase

### 구조 설계
- SOLID 원칙 준수
- 클래스와 메서드를 작은 단위로 유지하여 가독성 확보
- 비전공자도 이해하기 쉬운 코드 작성

### 주석
- 이해하기 어려운 모든 코드에 주석 추가
- 모든 메서드에 해당 메서드가 무엇을 하는지 자세한 설명 주석 추가
- 주석에 특수 아이콘(이모지 등) 사용 금지

## 추천 사항

### 실무 관점
- 실무에서 해당 기능을 어떻게 구현하는지 추천 및 설명할 것

### 최적화
- 성능 및 최적화를 고려한 구현 방법 추천할 것

## 금지 사항
- 파일 시스템 관련 작업 하지 말 것
- `DontDestroyOnLoad`를 사용하는 싱글톤 패턴 사용 자제
- 리플렉션 사용 자제

---

# 일반 행동 지침

LLM의 일반적인 코딩 실수를 줄이기 위한 행동 지침. 필요에 따라 프로젝트별 지침과 병합하여 사용할 것.

**트레이드오프:** 이 지침은 속도보다 신중함을 우선시한다. 사소한 작업에는 판단력을 발휘할 것.

## 1. 코딩 전 먼저 생각하기

**가정하지 말 것. 혼란을 숨기지 말 것. 트레이드오프를 드러낼 것.**

구현 전:
- 가정한 사항을 명시적으로 밝힐 것. 불확실하면 질문할 것.
- 해석이 여러 가지라면 제시할 것 - 조용히 선택하지 말 것.
- 더 단순한 접근법이 있다면 말할 것. 필요하면 반론을 제기할 것.
- 불명확한 부분이 있으면 멈출 것. 무엇이 혼란스러운지 명시하고 질문할 것.

## 2. 단순함 우선

**문제를 해결하는 최소한의 코드. 추측성 코드 금지.**

- 요청받은 것 이상의 기능 추가 금지.
- 단일 사용 코드에 추상화 금지.
- 요청하지 않은 "유연성"이나 "설정 가능성" 금지.
- 불가능한 시나리오에 대한 오류 처리 금지.
- 200줄을 작성했는데 50줄로 가능하다면 다시 작성할 것.

"시니어 엔지니어가 이게 과도하게 복잡하다고 할까?" 라고 자문할 것. 그렇다면 단순화할 것.

## 3. 외과적 변경

**반드시 필요한 부분만 건드릴 것. 자신이 만든 문제만 정리할 것.**

기존 코드 편집 시:
- 인접한 코드, 주석, 포맷을 "개선"하지 말 것.
- 고장나지 않은 것을 리팩토링하지 말 것.
- 다르게 하고 싶더라도 기존 스타일을 맞출 것.
- 관련 없는 죽은 코드를 발견하면 언급할 것 - 삭제하지 말 것.

변경으로 인해 고아 코드가 생기는 경우:
- 자신의 변경으로 인해 미사용이 된 import/변수/함수는 제거할 것.
- 요청받지 않는 한 기존의 죽은 코드는 제거하지 말 것.

기준: 변경된 모든 줄은 사용자의 요청과 직접 연결되어야 한다.

## 4. 목표 중심 실행

**성공 기준을 정의할 것. 검증될 때까지 반복할 것.**

작업을 검증 가능한 목표로 변환:
- "유효성 검사 추가" -> "잘못된 입력에 대한 테스트를 작성한 후 통과시킬 것"
- "버그 수정" -> "버그를 재현하는 테스트를 작성한 후 통과시킬 것"
- "X 리팩토링" -> "리팩토링 전후로 테스트가 통과하는지 확인할 것"

여러 단계의 작업에 대해서는 간략한 계획을 제시할 것:
```
1. [단계] -> 검증: [확인 방법]
2. [단계] -> 검증: [확인 방법]
3. [단계] -> 검증: [확인 방법]
```

명확한 성공 기준은 독립적으로 반복할 수 있게 한다. 모호한 기준("작동하게 만들기")은 지속적인 설명을 요구한다.

---

**이 지침이 효과적으로 작동한다면:** diff에 불필요한 변경이 줄어들고, 과도한 복잡성으로 인한 재작성이 줄어들며, 실수 후가 아닌 구현 전에 명확화 질문이 나온다.
